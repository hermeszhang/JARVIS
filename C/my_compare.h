#ifndef my_compare_h
#define my_compare_h

void my_compare(float(*)[], float(*)[], float(*)[], float (*)[], float(*)[]);
float my_sse(float(*)[], float(*)[]);
float quotient(float, float, float);

#endif

